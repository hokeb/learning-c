# Exercise 2.06

Why is not a good iea for an identifier to contain more than one adjacent underscore (as in `current__balance`, for example)?

## Solution

There is no real purpose to adding multiple underscores in between words. It makes it hard to read / reason about how many there are.
